# To add a dependency:
uv add PACKAGE

# To remove a package:
uv remove PACKAGE

# To run a script/command (verifies that the lockfile and environment are up-to-date with the pyproject.toml first):
uv run COMMAND

# To manually update the environment: 
uv sync
